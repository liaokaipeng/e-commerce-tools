# 新手入门指南

本工具在本地网页（`http://127.0.0.1:8765`）提供多个功能：**视频下载**（TikTok 无水印）、**竞价导出**（Shopee 数据 → Excel）、**取消竞价**（批量撤销「待改进」竞价）、**取消 Hot Listing**（按 SPU 批量取消已注册 SKU）、**视频上传**（短视频批量上传，分「跨境」「本土」两个入口）、**视频压缩**（把本地文件夹里的视频压到 30MB / 1 分钟以内），另有「**开放平台**」页（Shopee 开放平台 App 授权，为官方 API 功能统一管理登录）与「**监控大屏**」页（对已授权店铺定时巡检，P0/P1/P2 三级告警大屏展示）。

> **「视频下载」与「视频压缩」无需授权店铺**（不访问任何线上接口）；其余功能都要先完成一次「授权店铺」：竞价系三个工具、视频上传与监控大屏的店铺列表都来自开放平台授权，没授权就没有店铺可选。

## 准备

- Windows 电脑，已装 **Node.js**（≥18，<https://nodejs.org/>）
- 浏览器 **Edge** 或 **Chrome**

## 启动程序

1. 双击 **`启动.bat`**，等待黑窗口提示完成（首次会安装依赖并构建前端）。
2. 自动打开浏览器进入 `http://127.0.0.1:8765` 即成功。
3. **黑窗口不要关闭**，要退出时直接关掉它即可。

## 安装扩展（一次性）

竞价系三个工具（竞价导出 / 取消竞价 / 取消 Hot Listing）与视频上传需要扩展把网页里的登录信息送到本地。

1. 浏览器打开 `edge://extensions`（Edge）或 `chrome://extensions`（Chrome）。
2. 打开右上角「开发人员模式」→ 点「加载解压缩的扩展」，选择工具文件夹里的 **`extension`** 目录。
3. 安装后浏览器右上角出现「电商工具箱」图标。

## 授权店铺（一次性，必需）

竞价系三个工具、视频上传与监控大屏都要先做这一步：授权后工具才知道你名下有哪几个店铺，各页面的「选择店铺」列表就来自这里。

1. 打开网页「开放平台」Tab →「① App 配置」填入 App 的 `partner_id` / `partner_key`（生产环境）→ 保存（凭证只存本机）。
2. 「② 店铺授权」填回调地址（没有域名时，开放平台后台与本页都填 `https://example.com/`）→ 点「生成授权链接」→ 点「打开授权页」。
3. 用**主账号**登录并确认授权（一次授权名下全部店铺）。授权后浏览器会跳转，把地址栏完整链接复制回来，粘贴到「手动完成授权」。
4. 「③ 已授权店铺」列出店铺即成功；回各工具页点「刷新状态」即可勾选店铺。

> 授权链接约 30 分钟过期、授权码一次性；链接里只有 `code` 与 `main_account_id`、没有 `shop_id` 属正常情况。有域名时可在页面里放一个转发页，之后全自动跳回、免粘贴。

Token 只存本机；access\_token 约 4 小时有效、过期自动刷新，refresh\_token 约 30 天有效。店铺显示「需重新授权」时，重新做一次第 3 步即可恢复（原因是共享主账号 token 的店铺整组绑定，某店铺刷新后其余店铺的授权关系失效，需整组重新授权）；店铺多时可在「③ 已授权店铺」点「批量刷新 token」一键续期（会自动把共享同一主账号 token 的店铺归组、每组只刷一次；短时间内重复点会跳过，同一时刻只跑一个批量任务，不会越刷越坏）。

## 视频下载

无需账号。打开「视频下载」Tab → 每行粘贴一个 TikTok 链接 → 选保存目录 → 点「开始下载」（可随时「停止下载」）。大陆网络需开 VPN/代理，工具自动读取系统代理。

## 竞价导出

前提：已完成上面的「授权店铺」与「安装扩展」。

1. 登录卖家中心 `seller.shopee.cn`。
2. 点浏览器扩展图标 → 「发送登录信息到工具」。
3. 回网页「竞价导出」Tab → 刷新状态 → 选导出目录 → 勾选店铺 → 开始导出。

## 取消竞价

前提：已完成上面的「授权店铺」与「安装扩展」。

1. 登录卖家中心 `seller.shopee.cn`。
2. 点浏览器扩展图标 → 「发送登录信息到工具」。
3. 回网页「取消竞价」Tab → 刷新状态 → 勾选店铺 → 点「扫描待改进竞价」预览每条待撤销的竞价。
4. 确认无误后点「撤销全部待改进竞价」→ 再次确认 → 自动逐条撤销，日志实时显示进度。

> 撤销后竞价立即失效、不可恢复，请务必先扫描确认再执行。

## 取消注册 Hot Listing

前提：已完成上面的「授权店铺」与「安装扩展」。

1. 登录卖家中心 `seller.shopee.cn`，用扩展发送登录信息（与「取消竞价」同一步骤，两者共用同一份登录信息）。
2. 回网页「取消Hot Listing」Tab → 刷新状态 → 勾选店铺。
3. 每个店铺填 **1 个 SPU ID**（输入后自动保存，下次打开仍在）→ 点「扫描」预览该 SPU 下已注册的 SKU。
4. 确认无误后点「执行」，自动逐条取消注册，日志实时显示进度；执行中可暂停 / 继续 / 取消。

> 只处理资格正常的已注册 SKU，资格异常（如库存不达标）的行无法取消、会自动跳过。

## 视频批量上传

前提：已完成上面的「授权店铺」与「安装扩展」。

1. 打开对应入口：「跨境视频上传」Tab（.cn）或「本土视频上传」Tab（菲律宾 .ph）。
2. 去对应站点的短视频上传页手动传一次视频，扩展自动抓取凭证（无需手动填写 Cookie / Authorization）。
3. 选择 .xlsx 表格（表头：视频路径 / 视频说明 / 商品编码）→ 开始上传。

| 站点 | 需要操作 | 说明 |
|---|---|---|
| 跨境（.cn） | 选择店铺 | 默认自动选定已抓凭证的店铺，可改选；凭证账号级通用：任一店铺手动上传一次后，同账号下所有店铺（含不同国家站点）均可直接上传 |
| 本土-菲律宾（.ph） | User ID | 扩展通常自动抓取；为空时按 F12 → Network → `report/add` → Payload 的 `userId` 手动填写 |

## 视频压缩（把视频压到 30MB / 1 分钟以内）

**无需授权店铺、无需扩展，全程在本机完成，不会上传任何文件。** 适合发布前把一大批视频统一压到平台限制之内。

**第一次使用：装一次 ffmpeg**

压缩靠 `ffmpeg` 完成，工具本身不带（它体积大，放进去会让每次更新都多下 180MB）。

1. 打开门户页侧栏「其他」组的「**视频压缩**」Tab。
2. 顶部若显示黄色「**未检测到 ffmpeg**」，点「**下载并安装 ffmpeg**」，等进度走完（约 180MB，一般几十秒到几分钟，装一次以后都不用再装）。
3. 装完顶部变成绿色「ffmpeg 已就绪」即可。

> 如果你电脑里已经有 ffmpeg，也可以直接把它放到程序目录的 `bin\` 文件夹里（需要 `ffmpeg.exe` 和 `ffprobe.exe` 两个文件），再点「重新检测」，免去下载。

**日常使用**

1. 打开「视频压缩」Tab，在「**源文件夹**」里填（或点「浏览」选）要处理的文件夹。
2. 「**输出目录**」留空即可——默认把结果放在**源文件旁边**（同一个文件夹），文件名加 `-compressed` 后缀。
3. 点「**扫描文件夹**」，下面会列出该文件夹**及其所有子文件夹**里找到的视频（只列视频，其它文件自动忽略）。
4. 需要的话调「**压缩目标**」（默认已是 30MB / 60 秒）。确认后点「**开始压缩**」，进度、每个文件的结果都会实时显示在表格和日志里。

**压缩目标怎么设**

| 项目 | 默认 | 说明 |
|---|---|---|
| 单个文件体积上限 | 30 MB | 超出的会被重新编码压到限制内 |
| 单个文件时长上限 | 60 秒 | 超出的会被**整段加速**到这个时长以内（内容全保留，只是变快） |
| 输出方式 | 另存为（加 `-compressed` 后缀） | 改成「直接覆盖原文件」会用结果替换原片（原片会被删除、不可恢复，建议先备份） |
| 分辨率上限 | 长边 1280 | 分辨率过高的会先等比缩小（低码率下先缩分辨率比硬压更清楚）；也可选「不缩放」保持原分辨率 |

**结果放在哪**

- 默认**不会覆盖原文件**：产物放在源文件旁边，名字加 `-compressed`（例如 `产品视频.mp4` → `产品视频-compressed.mp4`），并且统一转成 `.mp4`。原视频一直在原处、不受影响。
- 若在「输出方式」选了「**直接覆盖原文件**」，压缩结果会替换掉原片——请先备份；一旦替换不可恢复。若压缩结果反而没有变小，工具会**保留原片不替换**，日志里会说明原因。
- 需要的话也可以填「**输出目录**」，指定后产物会写到那里，并**保持原有的子文件夹结构**。
- 已经同时满足「体积 ≤ 30MB 且时长 ≤ 60 秒」的视频会**自动跳过**（日志会写「已达标，跳过」）——它们本来就不用压，重编一遍只会白白掉画质。
- 若要打开产物所在的文件夹，点「**打开输出目录**」。

**执行中能做什么**

- 「**暂停**」：当前这个文件会先压完，然后停在下个文件之前；点「继续」恢复。
- 「**取消**」：立刻中断当前文件（没压完的不完整文件会被删掉），**已经压好的产物保留**。
- 同一时间只能跑一个压缩任务（压缩很吃 CPU，同时跑两个只会都变慢）。

**常见问题**

| 现象 | 解决 |
|---|---|
| 顶部一直显示「未检测到 ffmpeg」 | 点「下载并安装 ffmpeg」；网络不畅时可自己下载 ffmpeg，把 `ffmpeg.exe` 与 `ffprobe.exe` 放进程序目录的 `bin\` 文件夹，再点「重新检测」 |
| 下载 ffmpeg 中途失败 | 工具会自动换下一个下载源重试；全部失败说明网络不通，改用上面的手动放置办法 |
| 扫描出来是空的 | 该文件夹（含子文件夹）里确实没有视频文件；确认路径没填错，且视频扩展名是 mp4 / mov / mkv / avi / flv / wmv 等常见格式 |
| 某个文件显示「失败」 | 鼠标看该行的「结果」列有具体原因；常见是文件损坏、不是有效视频，或被别的程序占用 |
| 压缩后还是超过 30MB | 极少见（码率已降到下限仍压不进去，日志会标「仍超出体积上限」）；把「体积上限」调大一点，或把「分辨率上限」调低后重跑 |
| 视频变快了 | 源视频超过 60 秒，工具会把整段**加速**到 60 秒以内——内容全保留，只是变快（不会截掉开头） |
| 选了「直接覆盖原文件」但原片还在 | 压缩结果没有变小（或同名 `.mp4` 已存在）时，工具会保留原片不替换，日志里会写明原因 |
| 想重新压一批 | 直接再点「开始压缩」即可；工具会跳过名字带 `-compressed` 的产物，不会把成品再压一遍（要重压就先把那些产物删掉） |

## 监控大屏（多店巡检 · 三级告警）

完成上面「授权店铺」后即可使用，无需其它设置。

**怎么用**

1. 打开门户页「监控大屏」Tab，深色大屏即出现。
2. **打开大屏就自动开始巡检**，打开瞬间立即补一轮采集，不需要点任何按钮。
3. **按需采集**：只有大屏打开时才巡检，切走/关闭即暂停，回来立即续采；数据存本机，重启后继续积累。

大屏分四个区域：

- **顶栏统计卡**：店铺数 / P0 / P1 / P2 / 正常数量，P0 红色脉冲；可开关「投影」「声音」「轮播」，「金额」可切换当地货币 / 人民币展示。
- **店铺健康墙（左）**：每店一张卡片，颜色 = 最高未关闭告警级别，按 P0→P1→P2→正常自动置顶；点击聚焦该店。
- **趋势 + 矩阵（中）**：7 天趋势曲线 + 多店 × 指标对比矩阵；点店铺卡或矩阵行切换店铺，单元格底色 = 告警级别，红框 ✗ = 采集失败（悬浮看原因）。
- **告警流（右）**：按 P0→P1→P2 排序的告警列表，每条可「详情」「确认」「关闭」。

**监控什么**：对已授权店铺定时巡检六个域——订单履约（每 10 分钟）、商品库存（每 30 分钟）、账户健康 / 广告投放 / 资金结算 / 售后评价（每 60 分钟），共 22 项指标。例如今日待发货订单数、断货商品数、迟发货率、今日广告花费、近 24h 差评数等；全部阈值可在「告警规则」面板按自己店铺的实际水平调整，留空的级别不触发。

**三级告警怎么读**

| 级别 | 含义 | 视觉 | 响应建议 |
|---|---|---|---|
| P0 紧急 | 正在造成损失或威胁账户（大量断货/严重积压/采集长期失联） | 红色，置顶闪烁 + 声音 | 立即处理（分钟级） |
| P1 重要 | 业务受损正在发生 | 橙色，置顶 | 当日处理（小时级） |
| P2 提醒 | 趋势性风险、观察项 | 黄色 | 关注即可 |

同店同规则重复触发会去重计数（×N），恶化自动升级，回稳自动移出告警流；P2 持续 24 小时未恢复自动升 P1。点告警「详情」可看具体明细（断货/低库存是哪些商品、待发货是哪些订单、差评内容等），支持关键词过滤与一键复制。

**几个常用操作**

- **只想监控部分店铺**：顶栏「监控店铺」面板取消勾选即可，未勾选的店铺不采集、不进大屏，重新勾选立即恢复。
- **手动补一轮**：点「立即采集」按钮。
- **数据与隐私**：数据仅存本机，快照保留 90 天；不含凭证与买家敏感信息（订单只显示订单号与创建日期），采集全部走只读查询接口，不改动店铺任何数据。

**告警数与卖家中心不完全一致属正常**：待发货按订单号前 6 位日期估算（接口不返回创建时间）、库存为抽查最近更新的 50 个热点商品而非全量、迟发货/未完成率为平台周口径快照；阈值不贴合就在规则面板调整。

## 更新版本

工具会自动检查新版本（启动后约 5 秒检查一次，之后每 6 小时一次），你只需偶尔看一眼侧栏：

- 门户页**左侧底部**显示当前版本号（如 `v1.2.0`）。
- 出现橙色「**新 v1.3.0**」徽标时点它 → 弹出「版本与更新」抽屉，里面写着更新内容。
- 点「**一键更新**」，等进度走完 → **关闭黑色窗口，重新双击 `启动.bat`** 即生效。
- 若网络不畅导致一键更新失败，可点「下载安装包」手动解压覆盖（解压时**不要**覆盖或删除程序目录里的 `server/data` 文件夹，那是你的店铺授权数据），再重启。
- 更新前工具会自动把 `server/data` 备份到程序目录的 `backup/` 下，可随时找回。
- 若用了浏览器扩展且更新包含扩展改动，还需打开 `edge://extensions`（Chrome 为 `chrome://extensions`），在「电商工具箱」上点「重新加载」。
- 左侧版本号旁若显示「检查失败」，只是暂时连不上更新源，**不影响任何工具的使用**；点开抽屉可重试。

## 常见问题

| 现象 | 解决 |
|---|---|
| `启动.bat` 没反应 | 先装 Node.js ≥18 |
| 端口 8765 被占用 | 关掉旧黑窗口后重开 |
| **选不到店铺 / 店铺列表为空** | 还没授权：到「开放平台」页按上文「授权店铺」做一次 |
| 店铺显示「需重新授权」 | 到「开放平台」页重新生成授权链接、主账号授权一次；或在「③ 已授权店铺」点「批量刷新 token」续期 |
| TikTok 网络错误 | 开 VPN/代理后点「重新检测网络」 |
| 竞价 `Failed to fetch` | 服务没起，重跑 `启动.bat` |
| 竞价 403 / 登录失效 | 重新登录卖家中心 → 扩展重推 |
| 取消 Hot Listing 403 / 登录失效 | 与竞价共用登录信息：重新登录卖家中心 → 扩展重推 |
| 取消 Hot Listing 扫描为空 | 该 SPU 下没有「已注册且资格正常」的 SKU，或 SPU ID 填错 |
| 视频上传 401/403 / token is expired | 去**对应站点**短视频页再传一次，扩展自动刷新凭证 |
| 视频上传遇到临时网络波动 | 工具会自动重试（最多 3 次），无需手动操作；若最终仍失败，多半是网络不稳或凭证过期，检查网络或到对应站点短视频页重传一次 |
| 监控大屏店铺显示「采集连续失败」 | 多为授权失效：到「开放平台」页重新生成授权链接、主账号登录授权一次即可恢复 |
