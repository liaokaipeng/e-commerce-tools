# 新手入门指南

本工具在本地网页（`http://127.0.0.1:8765`）提供四个功能：**视频下载**（TikTok 无水印）、**竞价导出**（Shopee 数据 → Excel）、**取消竞价**（Shopee 批量撤销「待改进」竞价）、**视频上传**（Shopee 短视频批量上传，分「跨境视频上传」「本土视频上传」两个入口），另有「**开放平台**」页（完成 Shopee 开放平台 App 授权后，为官方 API 功能统一管理登录）与「**监控大屏**」页（对已授权店铺定时巡检，P0/P1/P2 三级告警大屏展示）。

## 准备

- Windows 电脑，已装 **Node.js**（≥18，https://nodejs.org/）
- 浏览器 **Edge** 或 **Chrome**

## 启动程序

1. 双击 **`启动.bat`**，等待黑窗口提示完成（首次会安装依赖并构建前端）。
2. 自动打开浏览器进入 `http://127.0.0.1:8765` 即成功。
3. **黑窗口不要关闭**，要退出时直接关掉它即可。

## 安装扩展（一次性）

竞价与视频上传需要扩展把网页里的登录信息送到本地。

1. 浏览器打开 `edge://extensions`（Edge）或 `chrome://extensions`（Chrome）。
2. 打开右上角「开发人员模式」→ 点「加载解压缩的扩展」，选择工具文件夹里的 **`extension`** 目录。
3. 安装后浏览器右上角出现「KP工具合集助手」图标。

## 视频下载

无需账号。打开「视频下载」Tab → 每行粘贴一个 TikTok 链接 → 选保存目录 → 点「开始下载」（可随时「停止下载」）。大陆网络需开 VPN/代理，工具自动读取系统代理。

## 竞价导出

1. 登录卖家中心 `seller.shopee.cn`。
2. 点浏览器扩展图标 → 「发送登录信息到本地工具」。
3. 回网页「竞价导出」Tab → 刷新状态 → 选导出目录 → 勾选店铺 → 开始导出。

## 取消竞价

对应人工流程「竞价详情 → 进行中的竞价 → 待改进 → 逐个撤销」，全自动完成：

1. 登录卖家中心 `seller.shopee.cn`。
2. 点浏览器扩展图标 → 「发送登录信息到本地工具」。
3. 回网页「取消竞价」Tab → 刷新状态 → 勾选店铺 → 点「扫描待改进竞价」预览每条待撤销的竞价。
4. 确认无误后点「撤销全部待改进竞价」→ 再次确认 → 自动逐条撤销，日志实时显示进度。

> 撤销后竞价立即失效、不可恢复，请务必先扫描确认再执行。

## 视频批量上传

1. 打开对应入口：「跨境视频上传」Tab（.cn）或「本土视频上传」Tab（菲律宾 .ph）。
2. 去对应站点的短视频上传页手动传一次视频，扩展自动抓取凭证（全程无需手动填写 Cookie / Authorization）。
3. 选择 .xlsx 表格（表头：视频路径 / 视频说明 / 商品编码）→ 开始上传。

| 站点 | 需要操作 | 说明 |
|------|---------|------|
| 跨境（.cn） | 选择店铺 | 默认自动选定已抓凭证的店铺，也可用「选择店铺」下拉框改选；凭证由扩展自动抓取，账号级通用：任一店铺手动上传一次后，同账号下所有店铺（含不同国家站点）均可直接上传 |
| 本土-菲律宾（.ph） | User ID | 扩展通常自动抓取；为空时按 F12 → Network → `report/add` → Payload 的 `userId` 手动填写 |

## 开放平台登录（官方 API 授权）

普通使用四个工具不需要这一步；后续基于 Shopee 官方开放平台的 API 功能（新工具）统一依赖这里的登录。操作：

1. 在 Shopee 开放平台（open.shopee.cn / open.shopee.com）注册开发者账号并创建一个 App，拿到 **partner_id** 与 **partner_key**（在 App 管理页查看）。
2. 在 App 管理后台把**回调地址（redirect）**保存为一个合法域名地址——后台只做域名校验，**不接受** `127.0.0.1` / `localhost` / `sslip.io` 等指向本机的地址，也不能带 `?` 查询参数。把你自己的域名地址填进去（http/https 均可，例如 `http://你的域名/openapi-callback/`），并把工具「开放平台」页的「回调地址」输入框填成**完全一样**的值。没有域名时，后台与工具两边都填默认的 `https://example.com/`（工具默认已是该值），授权后用「手动完成授权」粘贴。
3. 打开本工具「开放平台」Tab → 填写 partner_id / partner_key（环境一般选「生产环境」）→ 保存配置。
4. 点「生成授权链接」→ 复制链接在浏览器打开 → 登录卖家**主账号** → 授权（授权对象是主账号，其下所有店铺由官方随 token 一并返回，工具会自动全部保存）。
5. 授权后浏览器会跳到第 2 步填的回调地址，按以下任一方式完成登录：
   - **全自动（推荐，有域名时）**：按「开放平台」页「有域名？」折叠说明，在你的域名放一个转发页（页面代码工具里可直接复制），或给域名加一条指向 127.0.0.1 的 A 记录（如 `local.你的域名`），授权后自动跳回本工具换取 Token，无需任何粘贴。
   - **手动粘贴**：把浏览器地址栏里的完整链接复制到「开放平台」页「手动完成授权」输入框，点按钮即换取 Token。主账号授权的链接形如 `…?code=…&main_account_id=…`（**没有 shop_id 是正常的**）。

   「已授权店铺」列表出现该店铺即登录成功，可点「测试」验证（返回店铺名）。

> - 授权链接约 30 分钟过期、授权码一次性；失败或过期请回到「开放平台」页重新生成。
> - Token 只保存在本机（`server/data/openapi-session.json`，不上传）；access_token 约 4 小时有效（过期自动刷新）。主账号授权的共享 token 被某店铺刷新绑定后，其余店铺会显示「需重新授权」（工具会自动标记并暂停其采集，避免反复报错）——重新走一次本流程（主账号授权）即可全部恢复。
> - 沙箱环境仅供官方联调测试使用，实际店铺请用生产环境。

## 监控大屏（多店巡检 · 三级告警）

完成上面「开放平台登录」后即可使用，无需其它设置：

1. 打开「监控大屏」Tab → 自动开始对已授权店铺巡检采集（订单履约 10 分钟 / 商品库存 30 分钟 / 账户健康 60 分钟）。**按需采集**：只有大屏页面打开时才巡检，切到其它 Tab、关闭页面或最小化浏览器即暂停，回到大屏立即补采。
2. 大屏实时展示：顶部 P0/P1/P2/正常统计卡、左侧店铺健康墙（按告警级别自动置顶）、中间趋势图与多店对比矩阵、右侧分级告警流（可确认/关闭，回稳自动标「已恢复」）、底部采集状态条。
3. P0 紧急红色闪烁 + 声音提醒、P1 橙色、P2 黄色；阈值可在「告警规则」面板调整。
4. **只想监控部分店铺**：点顶栏「监控店铺」，取消勾选不需要的店铺即可（未勾选的不采集、不进大屏）。
5. **告警只在大屏展示**；保持工具（黑窗口/常开机器）运行、并在需要时打开大屏，才有巡检与告警。

指标口径、告警分级、常见问题与后续待办详见《[监控大屏](docs/监控大屏.md)》。

## 常见问题

| 现象 | 解决 |
|------|------|
| `启动.bat` 没反应 | 先装 Node.js ≥18 |
| 端口 8765 被占用 | 关掉旧黑窗口后重开 |
| TikTok 网络错误 | 开 VPN/代理后点「重新检测网络」 |
| 竞价 `Failed to fetch` | 服务没起，重跑 `启动.bat` |
| 竞价 403 / 登录失效 | 重新登录卖家中心 → 扩展重推 |
| 视频上传 401/403 / token is expired | 去**对应站点**短视频页再传一次，扩展自动刷新凭证 |
| 视频上传遇到临时网络波动 | 工具会自动重试（最多 3 次），无需手动操作；若最终仍失败，多半是网络不稳或凭证过期，检查网络或到对应站点短视频页重传一次刷新凭证 |
| 监控大屏店铺显示「采集连续失败」 | 多为授权失效：到「开放平台」页重新生成授权链接、主账号登录授权一次即可恢复 |