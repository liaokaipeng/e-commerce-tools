# 工具合集

将原先三个独立工具（TikTok 下载 / Shopee 竞价导出 / Shopee 视频上传）合并为**一个服务、一个端口（8765）、一个网页**，按 **Shopee / TikTok** 分类展示。

前端基于 **Vue3 + Element Plus + Vite**（多页应用），构建产物由后端托管。

打开 http://127.0.0.1:8765 后，在顶部 Tab 切换三个工具，无需分别启动多个窗口。

## 一键启动

双击 `启动.bat`（首次会自动安装后端依赖、安装并构建前端 `frontend/dist`，再自动打开浏览器）。

## 前端开发 / 构建

前端工程在 `frontend/` 目录（Vue3 + Element Plus + Vite 多页）。

```bash
# 开发模式（需先单独启动后端 main.js 提供 API）
cd frontend
npm install
npm run dev        # http://localhost:5173，/api 已代理到 8765

# 生产构建（产物输出到 frontend/dist，由 main.js 托管）
cd frontend
npm run build
```

修改前端代码后，需重新 `npm run build` 才能在 http://127.0.0.1:8765 生效（或删除 `frontend/dist` 后重跑 `启动.bat`）。

## 运行测试

```bash
node test.js
```

冒烟测试会临时启动一个服务实例到端口 8865，验证各页面与接口路由（不访问真实 TikTok/Shopee），约 16 项，全部通过即正常。

## 三个工具的使用

### 1. TikTok 无水印视频下载（Tab：TikTok 下载）
- 首次使用需在「选择保存位置」点「浏览」选择目录（也可直接输入路径），点「开始下载」时会询问是否设为默认目录。
- 设为默认后，下次打开自动回显该目录；也可随时点「设为默认目录」手动更新。
- 大陆网络访问 TikTok 通常需开 VPN/代理（工具自动读取系统代理）。

### 2. Shopee 竞价导出（Tab：竞价导出）
1. 安装浏览器扩展：`extension/` 目录（Edge：`edge://extensions` → 开发人员模式 → 加载解压缩的扩展）。
2. 登录 seller.shopee.cn，点扩展「发送登录信息到本地工具」，把 Cookie 推到本地（8765）。
3. 回到本页「竞价导出」Tab，点「刷新状态」，在「保存位置」点「浏览」选择导出目录（首次询问是否设为默认，之后自动回显），勾选店铺后点「开始导出」，生成 Excel 到该目录。

### 3. Shopee 视频批量上传（Tab：视频上传）
1. 打开 Shopee「短视频上传」页手动上传一次视频，扩展自动抓取 Cookie/ShopID 并推送到本地（8765）。
2. 回到本页「视频上传」Tab，选择 `.xlsx` 表格（表头含：视频路径 / 视频说明 / 商品编码），点「开始上传」。

## 端口与扩展说明

- 合并服务固定监听 **8765**（原 TikTok 8737、竞价 8765、视频上传 3000 已统一）。
- 三个工具只需加载**一个扩展**（`extension/` 目录，名为 **KP工具合集助手**），同时负责竞价 Cookie 推送与视频上传凭证抓取。
- 若此前已分别加载过「竞价导出助手」「视频上传凭证抓取」两个旧扩展，请将其**移除**，只保留本合集扩展，避免重复推送。

## 目录结构

```
工具合集/
  main.js           合并后端入口（单端口路由三个模块）
  bidding.js        Shopee 竞价导出模块
  video.js          Shopee 视频上传模块
  tiktok.js         TikTok 下载核心模块
  stores.json       竞价店铺列表（分类 / 名称 / 店铺ID，可直接增删而无需改代码）
  lib/
    http-utils.js   通用 HTTP 工具（JSON 响应 / 请求体读取 / 静态文件服务）
    http.js         统一出站请求封装（含按 host 的 Cookie 罐）
    settings.js     工具默认目录持久化（运行时生成 settings.json，不入库）
  frontend/         Vue3 + Element Plus + Vite 前端工程（多页）
    index.html      门户页（Tab 切换，三个 iframe 常驻以保留各 Tab 状态）
    tiktok/         TikTok 下载页面入口
    bidding/        Shopee 竞价导出页面入口
    video/          视频上传页面入口
    public/         静态资源（xlsx.full.min.js）
    src/            Vue 源码（各页 App.vue + 共享组件 DirPicker.vue）
    dist/           构建产物（由 main.js 托管，不入库）
  public/           旧版原生前端（Vite 未构建时的回退目录，可保留或删除）
```

## 常见问题

| 现象 | 解决 |
|------|------|
| 端口 8765 被占用 | 关掉旧的 node 黑窗口后重新双击 `启动.bat` |
| 竞导出 403 / 登录失效 | 重新登录卖家中心 → 扩展重推 Cookie |
| 视频上传 401/403 / token is expired | 凭证过期，去短视频上传页手动传一次视频，扩展自动刷新 |
| TikTok 下载网络错误 | 开 VPN/代理后重试 |